{
  # nixlytile_mode:
  #   1 = desktop only (normal window manager, ly login screen)
  #   2 = htpc only (auto-login, starts directly in HTPC mode,
  #                  htpc.nix activates: retroarch + jellyfin + HM configs)
  nixlytileMode = 2;
}
